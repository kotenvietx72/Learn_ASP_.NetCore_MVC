@{
    int[] x = new int[ViewBag.a];
    Html.BeginForm();
    {
        for (int i = 0; i < ViewBag.a; i++)
        {
            Html.Label("Nhập số thứ " + (i + 1) + " : ");
            Html.TextBox("x[i]");
        }
    }
    
}